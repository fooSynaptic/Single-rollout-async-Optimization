# MATH setting: grpo_dis_g1
# Uses env: SAO_WS, MODEL_ROOT, REWARD_ROOT, ADMIN_API_KEY


experiment_name: sao-math-instruct4b
trial_name: hard-grpo_dis_g1

seed: 1
enable_offload: false
total_train_epochs: 50
total_train_steps: 1000
tokenizer_path: ${actor.path}

cluster:
  n_nodes: 3
  n_gpus_per_node: 8
  fileroot: ${SAO_WS}/experiments
  name_resolve:
    type: nfs
    nfs_record_root: ${SAO_WS}/tmp/name_resolve/${experiment_name}/${trial_name}

scheduler:
  type: ray

rollout:
  backend: "vllm:d4p1t1"
  admin_api_key: ${ADMIN_API_KEY}
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  max_concurrent_rollouts: 32
  queue_size: null
  consumer_batch_size: ${train_dataset.batch_size}
  max_head_offpolicyness: 4
  enable_rollout_tracing: false
  scheduling_spec: ${actor.scheduling_spec}
  fileroot: ${cluster.fileroot}
  tokenizer_path: ${tokenizer_path}
  dump_to_file: false
  agent:
    mode: inline
    admin_api_key: ${ADMIN_API_KEY}
    export_style: individual
    turn_discount: 1.0

gconfig:
  n_samples: 1
  min_new_tokens: 0
  max_new_tokens: 512
  max_tokens: 1024
  greedy: false
  temperature: 1.0

actor:
  backend: "fsdp:d16p1t1"
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: ${MODEL_ROOT}/Qwen3-4B-Instruct-2507
  init_from_scratch: false
  disable_dropout: true
  gradient_checkpointing: true
  dtype: bfloat16
  attn_impl: sdpa
  mb_spec:
    max_tokens_per_mb: 16384
  optimizer:
    type: adam
    lr: 1.0e-6
    weight_decay: 0.0
    beta1: 0.9
    beta2: 0.999
    eps: 1.0e-8
    lr_scheduler_type: constant
    gradient_clipping: 1.0
    warmup_steps_proportion: 0.0
  eps_clip: 0.3
  eps_clip_higher: 5.0
  temperature: ${gconfig.temperature}
  reward_scaling: 1.0
  reward_bias: 0.0
  kl_ctl: 0.0
  ppo_n_minibatches: 1
  recompute_logprob: true
  use_decoupled_loss: true
  rejection_sampling:
    level: token
    action: mask
    metric: ratio
    lower: 0.3
    upper: 5.0
  # G=1: not group-relative (undefined); batch baseline + DIS
  reward_norm:
    mean_level: batch
    std_level: batch
  adv_norm:
    mean_level: batch
    std_level: batch
  weight_update_mode: disk
  max_new_tokens: ${gconfig.max_new_tokens}
  scheduling_spec:
    - task_type: worker
      port_count: 2
      gpu: 1
      mem: 8
      cmd: python3 -m areal.infra.rpc.rpc_server
      env_vars:
        NCCL_SOCKET_IFNAME: eth0
        GLOO_SOCKET_IFNAME: eth0
        TP_SOCKET_IFNAME: eth0
        NCCL_IB_DISABLE: "1"
        NCCL_NET: Socket

# No critic — DIS + batch reward norm only (G1 budget vs SAO)

ref:
  backend: ${actor.backend}
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: ${actor.path}
  init_from_scratch: false
  disable_dropout: true
  dtype: ${actor.dtype}
  attn_impl: sdpa
  mb_spec:
    max_tokens_per_mb: 16384
  optimizer: null
  scheduling_strategy:
    type: colocation
    target: actor
  scheduling_spec: ${actor.scheduling_spec}

vllm:
  model: ${actor.path}
  seed: ${seed}
  skip_tokenizer_init: false
  dtype: ${actor.dtype}
  max_model_len: 1024
  max_num_seqs: 64
  gpu_memory_utilization: 0.55

train_dataset:
  # G=1 · divisible by DP=16; 160 traj = 160 prompts × 1 sample
  batch_size: 160
  shuffle: true
  drop_last: true
  pin_memory: false
  num_workers: 4
  path: ${SAO_WS}/data/gsm8k_hard
  type: rl
  max_length: 512
  scheduling_spec: null

valid_dataset: null

saver:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  # 更密落盘，避免再中断时无 recover 只能冷启
  freq_secs: 1800

recover:
  mode: auto
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: 1800

evaluator:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: null
  freq_secs: null

stats_logger:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  wandb:
    mode: disabled
  swanlab:
    mode: disabled
  trackio:
    mode: disabled

perf_tracer:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  enabled: false
  session_tracer:
    enabled: false

reward:
  backend: fs_shard
  timeout_seconds: 30
  max_retries: 0
  on_error: raise
  fs_shard:
    root: ${REWARD_ROOT}
    shard_count: 2
    poll_interval_ms: 50
    heartbeat_stale_seconds: 15
    workers:
      - host: worker-0
        shard: 0
        concurrency: 48
      - host: worker-1
        shard: 1
        concurrency: 48
